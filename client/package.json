{
  "name": "fashion-erp-client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\"",
    "deploy": "gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.2",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.19"
  }
}
